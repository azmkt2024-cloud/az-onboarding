// 숫자 누락 2건(id=4, id=31) 보정 후 청크 저장
import fs from 'node:fs';

const fixes = {
  4: `처음 검사에서 의심되는 게 있어 다른 검사를 추가로 하거나, 정기적으로 다시 검사하는 경우가 모두 해당됩니다.
예) 박○○님이 가입 10개월 전 복통으로 응급실에서 피검사(원검사) 후 장염 소견을 받고, 5개월 전 같은 증상으로 복부 초음파(추가검사)를 받았다면 → 해당됩니다.
• 가입 9개월 전 혈압 약 처방 위해 정기적으로 혈압 체크(=재검사). 단, 유병자상품에서는 다르게 적용함에 유의.
• 가입 11개월 전 갑상선 초음파(원검사) 후 1년 이내 피검사(추가검사)를 한 경우.
• 가입 6개월 전 이석증 진단 후 3회 통원하며 2일간 여러 검사를 한 경우(=재검사).
• 가입 5개월 전 발목 X-ray(원검사) 후 피검사(추가검사)에서 요산수치로 통풍 의증 진통제 5일 처방.
• 가입 1개월 전 B형간염으로 10개월 전·5개월 전 피검사 등 정기 검진(=재검사).
• 가입 4개월 전 요통 X-ray(원검사) 후 가입 2개월 전 MRI(추가검사) 촬영.
• 가입 9개월 전 두통 피검사(원검사) 후 한 달 뒤 뇌 MRI(추가검사).
• 가입 전 유방 X-ray(원검사) 치밀 소견으로 8개월 전 초음파(추가검사).
• 가입 7개월 전 호흡곤란 X-ray(원검사) 후 5개월 전 흉부 CT(추가검사).
핵심: 진찰 중 질병이 의심되어 추가 검사를 하거나, 특이 소견 없이 정기적으로 내원해 검사하는 경우도 재검사에 포함됩니다.`,
  31: `조건에 따라 지급 가능합니다.
예) 김○○님이 1999.01.01 가입한 암진단비 2,500만원 보험에서, 얼굴의 C44.3 피부암이 일반암으로 지급될 수 있었습니다.
• 약관에 'C44.3 얼굴 피부암은 미미한 경우는 제외'라는 문구가 있어, 암이 얼마나 깊이 파고들었는지(침윤 정도)로 판단하기로 보험사와 합의했습니다.
• 의료자문 결과 피부 중 진피의 망상층까지 침윤이 확인되어, 미미한 경우가 아니라고 보아 보험금이 지급됐습니다.
핵심: 요즘 상품은 이런 피부암을 대부분 소액암으로 분류하니, 본인 약관을 꼭 확인하세요.`,
};

for (const n of [1, 2]) {
  const f = `scripts/_qna_out/chunk_${n}.json`;
  const arr = JSON.parse(fs.readFileSync(f, 'utf8'));
  let changed = false;
  for (const item of arr) {
    if (fixes[item.id]) {
      item.answer = fixes[item.id];
      changed = true;
      console.log(`patched id=${item.id}`);
    }
  }
  if (changed) fs.writeFileSync(f, JSON.stringify(arr, null, 2), 'utf8');
}
